# Classes and Object Model

This repository contains tasks and projects related to Object-Oriented Programming (OOP) in Python. 

## Task 0: Exploring Objects
Explored the nature of basic Python objects (like integers, strings, and lists) natively using Python's interactive interpreter (REPL), checking types (`type()`), identities (`id()`), and object behaviors/methods (`dir()`).
